# Neon Postgres connection string (pooled connection recommended for the web app).
# Get this from the Neon dashboard. Used by the scraper, drizzle migrations, and the web app.
DATABASE_URL="postgresql://user:password@ep-xxxx.eu-central-1.aws.neon.tech/neondb?sslmode=require"
